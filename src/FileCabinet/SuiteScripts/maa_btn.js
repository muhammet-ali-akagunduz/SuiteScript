define(['N/currentRecord'],
/**
 * 
 * @param {currentRecord} currentRecord 
 * @returns 
 */
    function(currentRecord) {
        function ali(){
            alert("Access Complate");
        }
        return {
            ali: ali
        };
    })